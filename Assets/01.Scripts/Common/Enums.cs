public enum EquipmentState
{
    Empty,
    Using,
    Dirty,
}

public enum GuestState
{
    Enter,
    DecideNext,
    MoveToZone,
    WaitInQueue,
    UseStation,
    DropMoney,
    Exit,
}

public enum LockerState
{
    Empty,
    Using,
}

public enum CounterState
{
    Empty,
    Using,
}